# Abduct

## installing

Fist install gems to sass.
```shell
$ gem install sass bourbon
```
then go to ``` dev/sass ``` and load bourbon files inside this folder
```shell
$ bourbon install
```
at last install dependencies from node

```shell
$ npm install
```